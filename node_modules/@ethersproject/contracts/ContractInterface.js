import { abi } from './abi';

class ContractInterface {
  constructor(abi) {
    this.abi = abi;
  }

  async encodeFunctionCall(func, args) {
    // implementation
  }

  async decodeFunctionResult(func, result) {
    // implementation
  }

  async getFunctionSelector(func) {
    // implementation
  }
}

export { ContractInterface };
