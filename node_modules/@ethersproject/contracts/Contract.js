import { abi } from './abi';
import { utils } from './utils';

class Contract {
  constructor(address, abi) {
    this.address = address;
    this.abi = abi;
  }

  async call(func, args) {
    // implementation
  }

  async sendTransaction(func, args) {
    // implementation
  }

  async estimateGas(func, args) {
    // implementation
  }

  async getStorageAt(slot) {
    // implementation
  }
}

export { Contract };
